import { Link, useLocation } from "react-router-dom";
import { useState } from "react";
import { siteConfig } from "@/lib/site-config";
import { Menu, X } from "lucide-react";
export function Navbar() { const [open,setOpen]=useState(false); const {pathname}=useLocation(); return <header className="sticky top-0 z-50 border-b border-white/10 bg-[#111313]/95 backdrop-blur"><div className="content-container flex h-18 min-h-[4.5rem] items-center justify-between gap-4"><Link to="/" className="font-bold tracking-tight text-[#f5f1e8] text-lg sm:text-xl"><span className="text-accent">CLEAR</span>ROAD <span className="text-xs font-medium text-muted-foreground">BAHAMAS</span></Link><nav className="hidden xl:flex items-center gap-5">{siteConfig.nav.slice(1).map(x=><Link key={x.href} to={x.href} className={pathname===x.href?"text-accent text-xs uppercase tracking-wider":"text-[#c9c7c1] hover:text-white text-xs uppercase tracking-wider"}>{x.label}</Link>)}</nav><Link to="/request-assessment" className="hidden sm:inline-flex bg-accent px-4 py-2.5 text-xs font-bold uppercase tracking-wider text-[#171717] hover:bg-[#f5c744]">Request a Site Assessment</Link><button className="xl:hidden p-2 text-white" aria-expanded={open} aria-label="Toggle navigation" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div>{open&&<nav className="xl:hidden border-t border-white/10 bg-[#111313]"><div className="content-container grid gap-4 py-6">{siteConfig.nav.map(x=><Link key={x.href} to={x.href} onClick={()=>setOpen(false)} className="text-sm text-[#e9e4d9]">{x.label}</Link>)}<Link to="/request-assessment" onClick={()=>setOpen(false)} className="bg-accent px-4 py-3 text-center text-sm font-bold text-[#171717]">Request a Site Assessment</Link></div></nav>}</header> }
